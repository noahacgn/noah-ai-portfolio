## Agent skills

### Issue tracker

Issue 使用仓库内 `.scratch/` 下的本地 Markdown 文件跟踪。详见 `docs/agents/issue-tracker.md`。

### Triage labels

Triage 使用五个默认规范标签。详见 `docs/agents/triage-labels.md`。

### Domain docs

领域文档采用单上下文布局。详见 `docs/agents/domain.md`。
