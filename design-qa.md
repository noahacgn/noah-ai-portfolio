# 后端与 AI 作品集设计验收

- 验收日期：2026-08-11
- 桌面视口：1440 × 1000
- 移动视口：390 × 844
- 窄屏视口：320 × 844
- 验收状态：首页、项目卡片、快捷入口与响应式顶栏

## 结论

- 未发现需要处理的 P0、P1 或 P2 问题。
- Hero 只保留 `Hey, I'm Noah Wang 👋` 与 `Backend & AI Portfolio`，不再显示额外职业定位行。
- 390px 视口下五个快捷入口全部完整位于首屏；最后一张 Contact 卡片底边为 835.1px，未超过 844px 视口。
- 320px 视口下隐藏居中的 NW 标记，完整保留招聘 CTA 和菜单；CTA 右边界为 260.2px，菜单范围为 260.2–298.2px，没有裁切或水平溢出。
- 三张项目插图均为 960 × 720 WebP，浏览器按 4:3 完整加载；桌面端约以 359 × 269.5px 展示，没有模糊、变形或裁剪。
- CPcash Wallet、Digimart、Quad Agent 三张部署图片分别为 68,864、64,976、66,020 字节；合计约 195KiB，相比原始 PNG 合计约 4.78MiB 减少约 96%。
- 项目卡片、状态、链接文案与正文保持清晰一致，浏览器控制台没有警告或错误。

## 验证记录

- [x] 删除首屏额外职业定位数据、DOM、样式和规格要求。
- [x] 将公开技能与问答中的 `Java 21` 统一为 `Java`。
- [x] 新增 390px 全部快捷入口完整可见的端到端回归测试。
- [x] 新增 320px 顶栏操作完整可见的端到端回归测试。
- [x] 按头像的约两倍最大展示尺寸原则，将三张项目 PNG 替换为 WebP。
- [x] 验证桌面三列、移动单列、图片自然尺寸和真实加载状态。
- [x] 通过类型检查、生产构建、Python 编译检查和 21 项端到端测试。

## 红—绿记录

1. 390px 首屏测试最初失败：Contact 只有约 42.7% 位于视口。删除额外职业定位行后，五张快捷卡全部完整可见。
2. 320px 顶栏测试最初失败：菜单只有约 81.0% 位于视口。窄屏隐藏居中的 NW 后，CTA 与菜单均完整可见。
3. Skills 测试最初无法找到精确的 `Java` 标签。公开资料统一替换后通过，并继续断言页面不显示 `Java 21`。

最终结果：通过。
